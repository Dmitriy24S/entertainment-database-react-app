import MediaList from '../../components/MediaList/MediaList'
import { MediaType } from '../../types'

const BookmarksListPage = () => {
  return <MediaList pageMediaType={MediaType.BOOKMARKS} />
}

export default BookmarksListPage
