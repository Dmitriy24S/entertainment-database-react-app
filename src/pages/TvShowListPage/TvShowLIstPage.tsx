import MediaList from '../../components/MediaList/MediaList'
import { MediaType } from '../../types'

const TvShowListPage = () => {
  return <MediaList pageMediaType={MediaType.TV} />
}

export default TvShowListPage
